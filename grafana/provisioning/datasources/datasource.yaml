apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: rag_user
    database: rag_monitoring
    secureJsonData:
      password: rag_password
    jsonData:
      sslmode: 'disable'
    isDefault: true
